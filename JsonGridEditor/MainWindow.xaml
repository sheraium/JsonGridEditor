<Window x:Class="JsonGridEditor.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:JsonGridEditor"
        mc:Ignorable="d"
        Loaded="MainWindow_OnLoaded"
        FontSize="16"
        Title="Json Grid Editor" Height="450" Width="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="60" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="10">
            <Button Content="Open CSV file" Click="ButtonOpen_OnClick"
                    Margin="5"
                    Width="150" Height="30" />
            <Button Content="Save" Click="ButtonSave_OnClick"
                    Margin="5"
                    Width="150" Height="30" />
        </StackPanel>
        <DataGrid x:Name="DataGrid1" Grid.Row="1"
                  ScrollViewer.CanContentScroll="True"
                  ScrollViewer.VerticalScrollBarVisibility="Auto"
                  ScrollViewer.HorizontalScrollBarVisibility="Auto" />
    </Grid>
</Window>